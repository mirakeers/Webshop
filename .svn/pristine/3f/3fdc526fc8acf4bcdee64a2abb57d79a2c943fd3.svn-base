$(document).ready(init);

function init() {
    $(".discard").click(function(e){e.preventDefault();if(window.confirm("Are you sure you want discard all changes and return to the admin page?")){window.location = "admin.php";}});
}